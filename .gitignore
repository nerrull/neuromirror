build/
documentation/*.mov
**.DS_Store**
mmpose/
greenlab/
freenect_test/models/
imgui.ini
mirror_panel.ini

# Sound work that lives outside the repo: BBC library downloads and the Reaper
# sessions that chop them up. Large, third-party or re-derivable, and nothing in
# the tree references either -- the piece's own recordings come in through
# WwiseProject.
audio/
reaper_proj/

# Instruments profiling sessions and crash dumps: large, machine-local, and
# not something anyone downstream needs.
WwiseProject/*.prof
mirror_app/crashlogs/

# Coverage builds and raw profile data: re-derivable, large, machine-local.
build_coverage/
coverage_profiles/
